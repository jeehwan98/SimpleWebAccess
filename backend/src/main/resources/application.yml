server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: prometheus,health
  endpoint:
    prometheus:
      access: unrestricted

aws:
  sqs:
    queue-url: ${SQS_QUEUE_URL:}
  s3:
    bucket: ${S3_BUCKET:simplewebaccess-prod-contacts}
  endpoint: ${AWS_ENDPOINT:}
